# Legacy vertical recovery code

This directory contains the old sequential vertical recovery, inside-bias
adaptation, sparse jump planner, vertical RTK preintegration feedback, and
reweighted IMU experiment code.

The current Phase 1 route does not build or call these files. They are kept
only as reference material while the main graph stays on the global optimization
path with split horizontal and vertical GNSS factors.
