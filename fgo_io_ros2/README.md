# fgo_io_ros2

This directory is intentionally a placeholder in phase 1.

Planned future scope:

- Ubuntu 24.04 + ROS 2 Jazzy adapter layer
- rosbag input support
- message-to-core adapters that do not leak ROS dependencies back into `fgo_core`

Not included in phase 1:

- `rclcpp`
- `rosbag2`
- plugin loading
- legacy online/offline node reuse
